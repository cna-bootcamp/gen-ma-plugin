PORT=3001
DMAP_PROJECT_DIR=/app/dmap-project
CORS_ORIGIN=http://localhost:8080

# 호스트 경로 설정 (docker-compose 볼륨 마운트용, 기본값 사용 시 주석 유지)
# CLAUDE_HOME_HOST=~/.claude
# DMAP_PROJECT_HOST_DIR=../../
# NGINX_PORT=8080
